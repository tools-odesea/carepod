<style>
  .cp-spotlight {
    position: relative;
    width: 100%;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 70vh;
    background: #0a0a0a;
  }

  .cp-spotlight__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    z-index: 0;
    opacity: 0.85;
  }

  /* Dark overlay to ensure text is readable */
  .cp-spotlight::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 1;
    background: rgba(0, 0, 0, 0.35);
  }

  .cp-spotlight__content {
    position: relative;
    z-index: 2;
    max-width: var(--page-width, 1200px);
    width: 100%;
    margin: 0 auto;
    padding: 6rem 4rem;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.25rem;
  }

  .cp-spotlight__eyebrow {
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.7);
    margin: 0;
  }

  .cp-spotlight__heading {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-family: var(--font-heading-family);
    font-style: var(--font-heading-style);
    font-weight: var(--font-heading-weight);
    line-height: 1.0;
    color: #ffffff;
    margin: 0;
    max-width: 640px;
  }

  .cp-spotlight__subheading {
    font-size: clamp(0.9rem, 1.5vw, 1.125rem);
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.85);
    margin: 0;
    max-width: 520px;
  }

  .cp-spotlight__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #ffffff;
    color: #0a0a0a;
    text-decoration: none;
    padding: 0.875rem 2.25rem;
    border-radius: 3px;
    font-size: 0.9375rem;
    font-weight: 600;
    letter-spacing: 0.01em;
    margin-top: 0.5rem;
    border: 1px solid #ffffff;
    transition: background 0.2s ease, color 0.2s ease;
  }

  .cp-spotlight__button:hover {
    background: transparent;
    color: #ffffff;
  }

  @media screen and (max-width: 749px) {
    .cp-spotlight {
      min-height: 100svh;
      align-items: flex-start;
    }

    .cp-spotlight__content {
      padding: 3.5rem 1.5rem;
      gap: 1rem;
    }

    .cp-spotlight__heading {
      font-size: clamp(2rem, 10vw, 2.75rem);
    }

    .cp-spotlight__bg--desktop { display: none; }
  }

  @media screen and (min-width: 750px) {
    .cp-spotlight__bg--mobile { display: none; }
  }
</style>

<div class="cp-spotlight">

  {%- if section.settings.image_desktop != blank -%}
    {{
      section.settings.image_desktop
      | image_url: width: 3840
      | image_tag:
        class: 'cp-spotlight__bg cp-spotlight__bg--desktop',
        sizes: '100vw',
        widths: '750, 1100, 1500, 2000, 3000, 3840',
        loading: 'lazy',
        alt: section.settings.heading | escape
    }}
  {%- endif -%}

  {%- assign mobile_img = section.settings.image_mobile | default: section.settings.image_desktop -%}
  {%- if mobile_img != blank -%}
    {{
      mobile_img
      | image_url: width: 1500
      | image_tag:
        class: 'cp-spotlight__bg cp-spotlight__bg--mobile',
        sizes: '100vw',
        widths: '375, 550, 750, 1100',
        loading: 'lazy',
        alt: section.settings.heading | escape
    }}
  {%- endif -%}

  <div class="cp-spotlight__content">
    {%- if section.settings.eyebrow != blank -%}
      <p class="cp-spotlight__eyebrow">{{ section.settings.eyebrow | escape }}</p>
    {%- endif -%}

    {%- if section.settings.heading != blank -%}
      <h2 class="cp-spotlight__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}

    {%- if section.settings.subheading != blank -%}
      <p class="cp-spotlight__subheading">{{ section.settings.subheading | escape }}</p>
    {%- endif -%}

    {%- if section.settings.button_label != blank -%}
      <a href="{{ section.settings.button_link | default: '#' }}" class="cp-spotlight__button">
        {{- section.settings.button_label | escape -}}
      </a>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Product Spotlight",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Images"
    },
    {
      "type": "image_picker",
      "id": "image_desktop",
      "label": "Desktop background image"
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "Mobile background image",
      "info": "Optional. Falls back to desktop image if blank."
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow text",
      "info": "Small text above the heading.",
      "default": "New"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Cube Plus"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "The world's cleanest cool-and-warm-mist humidifier, with our patented air hydration technology."
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Shop Cube Plus"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "presets": [
    {
      "name": "Product Spotlight"
    }
  ]
}
{% endschema %}
